@extends('layouts.portal') @section('page-title','Workflow settings') @section('section','Journal workflow')
@section('content')<form class="portal-card" method="post" action="{{ route('workflow.settings.save') }}">@csrf<div class="portal-card-body portal-form"><p>Formats must contain {year} and {sequence}. Article formats may also contain {volume} and {issue}. Changes affect future identifiers only.</p>@foreach(['manuscript_format','article_format','doi_agency','notification_intro'] as $field)<label>{{ str($field)->headline() }}<input class="portal-input" name="{{ $field }}" value="{{ $settings[$field] }}" required></label>@endforeach @foreach($settings['deadlines'] as $key=>$days)<label>{{ str($key)->headline() }} deadline (days)<input class="portal-input" type="number" min="1" max="365" name="deadlines[{{ $key }}]" value="{{ $days }}" required></label>@endforeach<p>DOI registration is manual. Future provider credentials belong in deployment environment settings.</p><button class="portal-button primary">Save settings</button></div></form>@endsection

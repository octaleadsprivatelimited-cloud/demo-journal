@extends('layouts.portal') @section('title','Reviewer feedback') @section('section','Author studio') @section('eyebrow','Shared reports') @section('page-title','Reviewer feedback')
@section('page-description','Completed reports and author-visible, location-specific comments across your manuscripts.')
@section('content')<div class="portal-form">@forelse($reviews as $review)<article class="portal-card"><div class="portal-card-head"><div><h2><a href="{{ route('author.articles.show',$review->article) }}">{{ $review->article->title }}</a></h2><p>Round {{ $review->submission?->round ?? '—' }} · Submitted {{ $review->completed_at->format('d M Y') }}</p></div><x-portal.status :value="$review->recommendation" /></div><div class="portal-card-body"><p style="white-space:pre-wrap;line-height:1.7">{{ $review->comments_to_author }}</p>@foreach($review->comments as $comment)<blockquote style="border-left:3px solid var(--portal-mint);padding-left:1rem"><strong style="font-size:.75rem">{{ $comment->location ?: 'General' }}</strong><p>{{ $comment->body }}</p></blockquote>@endforeach</div></article>@empty<div class="portal-card"><x-portal.empty title="No completed reports" message="Author-visible feedback appears once a reviewer submits their final recommendation." /></div>@endforelse<div>{{ $reviews->links() }}</div></div>@endsection

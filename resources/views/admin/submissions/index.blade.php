@extends('layouts.portal') @section('title','Submissions') @section('section','Editorial') @section('eyebrow','Editorial intake') @section('page-title','Submissions') @section('page-description','Track every submission round and frozen version from intake through final decision.')
@section('content')<div class="portal-card"><form class="filter-bar" method="get"><div class="portal-field"><label for="status">Status</label><select class="portal-select" id="status" name="status"><option value="">All</option>@foreach(\App\Enums\SubmissionStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ str($status->value)->replace('_',' ')->headline() }}</option>@endforeach</select></div><button class="portal-button" type="submit">Filter</button></form>@if($submissions->isEmpty())<x-portal.empty title="No submissions" message="Author submissions will appear here." />@else<div class="portal-table-wrap"><table class="portal-table"><thead><tr><th>Article</th><th>Submitter</th><th>Round</th><th>Status</th><th>Submitted</th><th></th></tr></thead><tbody>@foreach($submissions as $submission)<tr><td><a class="row-title" href="{{ route('admin.submissions.show',$submission) }}">{{ $submission->article->title }}</a><small>Article: {{ str($submission->article->status->value)->replace('_',' ')->headline() }}{{ $submission->article->trashed() ? ' · Archived' : '' }}</small></td><td>{{ $submission->submitter?->name??'System' }}<small>{{ $submission->submitter?->email }}</small></td><td>{{ $submission->round }}</td><td><x-portal.status :value="$submission->status" /></td><td>{{ $submission->submitted_at->format('d M Y, H:i') }}</td><td><a class="portal-button small" href="{{ route('admin.submissions.show',$submission) }}">Open</a></td></tr>@endforeach</tbody></table></div><div class="pagination-wrap">{{ $submissions->links() }}</div>@endif</div>@endsection

@php $editing=isset($tag); @endphp<form class="portal-form" method="post" action="{{ $editing?route('admin.tags.update',$tag):route('admin.tags.store') }}">@csrf @if($editing)@method('put')@endif<div class="portal-field"><label for="name">Name</label><input class="portal-input" id="name" name="name" value="{{ old('name',$tag->name??'') }}" required><x-portal.field-error name="name" /></div><div class="portal-field"><label for="slug">Slug <span>generated when blank</span></label><input class="portal-input" id="slug" name="slug" value="{{ old('slug',$tag->slug??'') }}"><x-portal.field-error name="slug" /></div><div class="portal-field"><label for="description">Description</label><textarea class="portal-textarea" id="description" name="description" rows="5">{{ old('description',$tag->description??'') }}</textarea></div><div class="form-actions"><a class="portal-button" href="{{ route('admin.tags.index') }}">Cancel</a><button class="portal-button primary" type="submit">{{ $editing?'Save tag':'Create tag' }}</button></div></form>

@extends('layouts.portal') @section('title','Media library') @section('section','Publishing') @section('eyebrow','Managed assets') @section('page-title','Media library') @section('page-description','Images are public and optimised for presentation; documents remain private behind authorised downloads.')
@section('content')<div class="dashboard-grid" style="grid-template-columns:minmax(280px,.55fr) minmax(0,1.45fr);align-items:start"><aside class="portal-card"><div class="portal-card-head"><h2>Upload media</h2></div><div class="portal-card-body"><form class="portal-form" method="post" action="{{ route('admin.media.store') }}" enctype="multipart/form-data" data-loading>@csrf<div class="portal-field"><label for="file">File <span>JPG, PNG, WebP, SVG, PDF or Word · images losslessly compressed to 1 MB maximum</span></label><input class="portal-input" id="file" name="file" type="file" accept=".jpg,.jpeg,.png,.webp,.svg,.pdf,.doc,.docx" required><x-portal.field-error name="file" /></div><div class="portal-field"><label for="alt_text">Alt text <span>required in practice for images</span></label><input class="portal-input" id="alt_text" name="alt_text"></div><div class="portal-field"><label for="caption">Caption</label><textarea class="portal-textarea" id="caption" name="caption" rows="3"></textarea></div><div class="portal-field"><label for="collection">Collection</label><input class="portal-input" id="collection" name="collection" value="library"></div><button class="portal-button primary" type="submit">Upload securely</button></form></div></aside><div class="portal-card"><form class="filter-bar" method="get"><div class="portal-field search-field"><label for="q">Search media</label><input class="portal-input" id="q" name="q" value="{{ request('q') }}" placeholder="Filename or alt text"></div><div class="portal-field"><label for="type">Type</label><select class="portal-select" id="type" name="type"><option value="">All files</option><option value="image" @selected(request('type')==='image')>Images</option><option value="application" @selected(request('type')==='application')>Documents</option></select></div><button class="portal-button" type="submit">Filter</button></form>@if($mediaItems->isEmpty())<x-portal.empty title="No media found" message="Upload a validated image or private document." />@else<div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(190px,1fr));gap:1px;background:var(--portal-line)">@foreach($mediaItems as $media)<article style="background:#fff;padding:1rem;min-width:0">@if($media->visibility==='public'&&str_starts_with($media->mime_type,'image/'))<img src="{{ Storage::disk($media->disk)->url($media->path) }}" alt="{{ $media->alt_text }}" style="aspect-ratio:4/3;object-fit:cover;width:100%;border-radius:7px">@else<div style="align-items:center;aspect-ratio:4/3;background:var(--portal-paper);border-radius:7px;display:flex;justify-content:center"><x-portal.icon name="article" :size="36" /></div>@endif<h3 style="font-size:.78rem;overflow:hidden;text-overflow:ellipsis;white-space:nowrap" title="{{ $media->original_name }}">{{ $media->original_name }}</h3><p class="portal-help">{{ strtoupper($media->extension) }} · {{ number_format($media->size_bytes/1024,1) }} KB · {{ $media->visibility }}</p><div class="row-actions"><a class="portal-button small" href="{{ route('admin.media.edit',$media) }}">Edit</a><a class="portal-button small" href="{{ route('admin.media.download',$media) }}">Download</a></div></article>@endforeach</div><div class="pagination-wrap">{{ $mediaItems->links() }}</div>@endif</div></div>@endsection

@extends('layouts.portal') @section('title','Edit media') @section('section','Publishing') @section('eyebrow','Managed asset') @section('page-title','Edit media') @section('page-description',$media->original_name)
@section('page-actions')<a class="portal-button" href="{{ route('admin.media.download',$media) }}"><x-portal.icon name="download" :size="17" />Download</a>@endsection
@section('content')<div class="portal-card" style="max-width:760px"><div class="portal-card-body"><form class="portal-form" method="post" action="{{ route('admin.media.update',$media) }}" enctype="multipart/form-data" data-loading>@csrf @method('put')<div class="portal-field"><label for="file">Replace file <span>optional; preserves visibility rules</span></label><input class="portal-input" id="file" name="file" type="file" accept=".jpg,.jpeg,.png,.webp,.svg,.pdf,.doc,.docx"><x-portal.field-error name="file" /></div><div class="portal-field"><label for="alt_text">Alt text</label><input class="portal-input" id="alt_text" name="alt_text" value="{{ old('alt_text',$media->alt_text) }}"></div><div class="portal-field"><label for="caption">Caption</label><textarea class="portal-textarea" id="caption" name="caption" rows="4">{{ old('caption',$media->caption) }}</textarea></div><div class="portal-field"><label for="collection">Collection</label><input class="portal-input" id="collection" name="collection" value="{{ old('collection',$media->collection) }}"></div><div class="form-actions"><a class="portal-button" href="{{ route('admin.media.index') }}">Cancel</a><button class="portal-button primary" type="submit">Save media</button></div></form><form method="post" action="{{ route('admin.media.destroy',$media) }}" data-confirm="Permanently delete this media file?" style="margin-top:1rem">@csrf @method('delete')<button class="portal-button danger" type="submit">Delete file</button></form></div></div>@endsection

@extends('layouts.portal') @section('title','Reviews') @section('section','Editorial') @section('eyebrow','Peer-review operations') @section('page-title','Reviews') @section('page-description','Monitor assignments, deadlines, progress, final recommendations, and reviewer history.')
@section('content')<div class="portal-card"><form class="filter-bar" method="get"><div class="portal-field"><label for="status">Status</label><select class="portal-select" id="status" name="status"><option value="">All</option>@foreach(\App\Enums\ReviewStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ str($status->value)->replace('_',' ')->headline() }}</option>@endforeach</select></div><button class="portal-button" type="submit">Filter</button></form>@if($reviews->isEmpty())<x-portal.empty title="No reviews found" message="Reviewer assignments will appear here." />@else<div class="portal-table-wrap"><table class="portal-table"><thead><tr><th>Article</th><th>Reviewer</th><th>Status</th><th>Due</th><th>Recommendation</th><th></th></tr></thead><tbody>@foreach($reviews as $review)<tr><td><a class="row-title" href="{{ route('admin.reviews.show',$review) }}">{{ $review->article->title }}</a></td><td>{{ $review->reviewer->name }}<small>{{ $review->reviewer->email }}</small></td><td><x-portal.status :value="$review->status" /></td><td>{{ $review->due_at?->format('d M Y')??'—' }}</td><td>{{ $review->recommendation?str($review->recommendation->value)->replace('_',' ')->headline():'—' }}</td><td><a class="portal-button small" href="{{ route('admin.reviews.show',$review) }}">Open</a></td></tr>@endforeach</tbody></table></div><div class="pagination-wrap">{{ $reviews->links() }}</div>@endif</div>@endsection

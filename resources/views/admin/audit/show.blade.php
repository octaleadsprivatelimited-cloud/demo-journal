@extends('layouts.portal') @section('title','Audit event') @section('section','Audience & system') @section('eyebrow','Accountability') @section('page-title','Audit event #'.$log->id) @section('page-description',$log->created_at->format('d M Y, H:i:s').' · '.($log->actor?->name??'System')) @section('page-actions')<a class="portal-button" href="{{ route('admin.audit.index') }}">Back to audit log</a>@endsection
@section('content')<div class="portal-card"><div class="portal-card-head"><h2>{{ str($log->event)->replace('_',' ')->headline() }}</h2></div><div class="portal-card-body"><dl class="detail-list"><div><dt>Actor</dt><dd>{{ $log->actor?->name??'System' }} · {{ $log->actor?->email }}</dd></div><div><dt>Auditable</dt><dd>{{ $log->auditable_type }} #{{ $log->auditable_id }}</dd></div><div><dt>Request ID</dt><dd>{{ $log->request_id??'—' }}</dd></div><div><dt>IP address</dt><dd>{{ $log->ip_address??'—' }}</dd></div><div><dt>Description</dt><dd>{{ $log->description??'—' }}</dd></div></dl><div class="form-grid"><div><h3>Before</h3><pre style="background:var(--portal-paper);overflow:auto;padding:1rem">{{ json_encode($log->old_values,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) }}</pre></div><div><h3>After</h3><pre style="background:var(--portal-paper);overflow:auto;padding:1rem">{{ json_encode($log->new_values,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) }}</pre></div></div></div></div>@endsection

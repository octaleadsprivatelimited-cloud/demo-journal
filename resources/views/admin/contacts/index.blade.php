@extends('layouts.portal') @section('title','Contact enquiries') @section('section','Audience & system') @section('eyebrow','Reader correspondence') @section('page-title','Contact enquiries') @section('page-description','Triage, assign, reply to, close, archive, and export reader correspondence.') @section('page-actions')<a class="portal-button" href="{{ route('admin.contacts.export') }}"><x-portal.icon name="download" :size="17" />Export CSV</a>@endsection
@section('content')<div class="portal-card"><form class="filter-bar" method="get"><div class="portal-field search-field"><label for="q">Search enquiries</label><input class="portal-input" id="q" name="q" value="{{ request('q') }}" placeholder="Name, email, subject…"></div><div class="portal-field"><label for="status">Status</label><select class="portal-select" id="status" name="status"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ str($status->value)->headline() }}</option>@endforeach</select></div><button class="portal-button" type="submit">Filter</button></form>@if($contacts->isEmpty())<x-portal.empty title="No enquiries found" message="New contact submissions will appear here." />@else<div class="portal-table-wrap"><table class="portal-table"><thead><tr><th>Sender</th><th>Subject</th><th>Status</th><th>Assignee</th><th>Received</th><th></th></tr></thead><tbody>@foreach($contacts as $contact)<tr><td><a class="row-title" href="{{ route('admin.contacts.show',$contact) }}">{{ $contact->name }}</a><small>{{ $contact->email }}</small></td><td>{{ $contact->subject }}<small>{{ str($contact->message)->limit(75) }}</small></td><td><x-portal.status :value="$contact->status" /></td><td>{{ $contact->assignee?->name??'Unassigned' }}</td><td>{{ $contact->created_at->format('d M Y, H:i') }}</td><td><a class="portal-button small" href="{{ route('admin.contacts.show',$contact) }}">Open</a></td></tr>@endforeach</tbody></table></div><div class="pagination-wrap">{{ $contacts->links() }}</div>@endif</div>@endsection
